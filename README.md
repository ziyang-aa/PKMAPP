# PKMAPP

宝可梦森林绘本风个人记账 Android 应用。

## 运行原型

在项目根目录执行：

```bash
./gradlew assembleDebug
./gradlew testDebugUnitTest
```

调试 APK 位于 `app/build/outputs/apk/debug/app-debug.apk`。

当前阶段提供连续横向首页、固定五项底栏和五个功能页壳层。页面仅使用临时前端状态，尚未接入数据库、网络或正式记账功能。
